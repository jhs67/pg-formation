tables:
  yaml:
    columns:
      - name: id
        type: id
      - name: text
        type: timestamptz
      - name: time
        type: INTEGER

    indices:
      - columns: text
      - columns:
        - id
        - time
types:
  foofara:
    values:
      - a
      - b
      - c
